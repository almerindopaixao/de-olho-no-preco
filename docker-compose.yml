services:
  mongodb:
    image: mongo:8.0
    container_name: de_olho_no_preco_db
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - ./mongo_data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}

  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: de_olho_no_preco_n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_RUNNERS_ENABLED=true
      - TZ=America/Sao_Paulo
      - GENERIC_TIMEZONE=America/Sao_Paulo
    volumes:
      - ./n8n_data:/home/node/.n8n
    depends_on:
      - mongodb

  # offer_hunter_api:
  #   build: ./offer_hunter_api
  #   container_name: offer_hunter_api
  #   restart: unless-stopped
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - MONGO_URI=${MONGO_URI}
  #     - N8N_WEBHOOK_URL=${N8N_WEBHOOK_URL}
  #   depends_on:
  #     - mongodb
  #     - n8n